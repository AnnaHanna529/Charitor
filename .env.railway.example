# НОВАЯ база (куда вставляем данные)
# Скопируйте:  copy .env.railway.example .env.railway
# Railway → новый проект → MySQL → Connect → Public Network

RAILWAY_MYSQL_URL=mysql://root:НОВЫЙ_ПАРОЛЬ@acela.proxy.rlwy.net:50987/railway

# Файл для импорта (после export-railway-db.js):
# RAILWAY_SQL_FILE=backup_railway.sql
